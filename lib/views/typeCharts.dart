import 'package:flutter/material.dart';

class TypeCharts extends StatelessWidget {
  const TypeCharts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
